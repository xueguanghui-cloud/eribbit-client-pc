interface window {
  name: string;
  level: string;
  code: number;
  areaList: window[];
}
interface Window {
  cityData: {
    areaList: [{}];
    code: string;
    level: number;
    name: string;
  };
}
